<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>seuPoint – IA, Economia e Mercado em um Ebook Prático</title>
  <meta name="description" content="Ebook completo sobre inteligência artificial e economia. Entenda o impacto no mercado, nas carreiras e nos negócios. Conteúdo prático, direto e aplicável.">
  <meta name="robots" content="index,follow">

  <style>
  @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700;800&display=swap');

  :root {
    --navy: #0D1B3E;
    --teal: #00E5D4;
    --steel: #3A7CA5;
    --white: #FFFFFF;
    --slate: #8898AA;
    --black: #0A0A0A;
    --grid: #1A1A1A;
    --card: #111111;
    --glow: rgba(0, 229, 212, 0.12);
    --glow-strong: rgba(0, 229, 212, 0.22);
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Space Grotesk', sans-serif;
    background-color: var(--black);
    color: var(--white);
    background-image:
      linear-gradient(var(--grid) 1px, transparent 1px),
      linear-gradient(90deg, var(--grid) 1px, transparent 1px);
    background-size: 28px 28px;
    overflow-x: hidden;
  }

  .teal { color: var(--teal); }
  .slate { color: var(--slate); }

  .section-label {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 3px;
    text-transform: uppercase;
    color: var(--teal);
    display: flex;
    align-items: center;
    gap: 12px;
    margin-bottom: 20px;
  }

  .section-label::before {
    content: '';
    width: 24px;
    height: 1px;
    background: var(--teal);
  }

  /* BOTOES */
  .btn-cta {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--teal);
    color: var(--black);
    font-family: 'Space Grotesk', sans-serif;
    font-size: 15px;
    font-weight: 800;
    letter-spacing: 0.5px;
    padding: 18px 36px;
    border-radius: 6px;
    text-decoration: none;
    border: none;
    cursor: pointer;
    transition: all 0.2s ease;
    position: relative;
    overflow: hidden;
  }

  .btn-cta::before {
    content: '';
    position: absolute;
    inset: 0;
    background: rgba(255,255,255,0.15);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
  }

  .btn-cta:hover::before { transform: translateX(0); }
  .btn-cta:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 40px var(--glow-strong);
  }

  .btn-outline {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: transparent;
    color: var(--white);
    font-family: 'Space Grotesk', sans-serif;
    font-size: 15px;
    font-weight: 600;
    padding: 17px 36px;
    border-radius: 6px;
    text-decoration: none;
    border: 1px solid rgba(255,255,255,0.15);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .btn-outline:hover {
    border-color: var(--teal);
    color: var(--teal);
  }

  /* NAVBAR */
  nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    padding: 20px 32px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(10,10,10,0.88);
    backdrop-filter: blur(14px);
    border-bottom: 1px solid var(--grid);
  }

  .nav-logo {
    font-size: 22px;
    text-decoration: none;
    color: var(--white);
  }

  .nav-logo span:first-child { font-weight: 300; }
  .nav-logo span:last-child {
    font-weight: 800;
    color: var(--teal);
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 32px;
    list-style: none;
  }

  .nav-links a {
    font-size: 13px;
    font-weight: 500;
    color: var(--slate);
    text-decoration: none;
    letter-spacing: 0.5px;
    transition: color 0.2s;
  }

  .nav-links a:hover { color: var(--white); }

  .nav-cta {
    font-size: 13px !important;
    font-weight: 700 !important;
    color: var(--teal) !important;
    border: 1px solid var(--teal);
    padding: 8px 20px;
    border-radius: 4px;
    transition: all 0.2s !important;
  }

  .nav-cta:hover {
    background: var(--teal) !important;
    color: var(--black) !important;
  }

  .hamburger {
    display: none;
    flex-direction: column;
    gap: 5px;
    cursor: pointer;
    padding: 4px;
    background: transparent;
    border: none;
  }

  .hamburger span {
    width: 22px;
    height: 2px;
    background: var(--white);
    border-radius: 2px;
    transition: all 0.3s;
  }

  .mobile-menu {
    display: none;
    position: fixed;
    top: 65px;
    left: 0;
    right: 0;
    background: rgba(10,10,10,0.97);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--grid);
    padding: 24px 32px;
    z-index: 99;
    flex-direction: column;
    gap: 20px;
  }

  .mobile-menu.open { display: flex; }

  .mobile-menu a {
    font-size: 16px;
    font-weight: 500;
    color: var(--slate);
    text-decoration: none;
    padding: 8px 0;
    border-bottom: 1px solid var(--grid);
    transition: color 0.2s;
  }

  .mobile-menu a:hover { color: var(--teal); }

  .mobile-menu .btn-cta {
    margin-top: 8px;
    justify-content: center;
    font-size: 14px;
  }

  /* HERO */
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 120px 32px 80px;
    position: relative;
    overflow: hidden;
  }

  .hero-glow-tr {
    position: absolute;
    top: -80px;
    right: -80px;
    width: 500px;
    height: 500px;
    background: radial-gradient(circle, var(--glow) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-glow-bl {
    position: absolute;
    bottom: -80px;
    left: -80px;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, var(--glow) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-inner {
    max-width: 1100px;
    margin: 0 auto;
    width: 100%;
    display: grid;
    grid-template-columns: 1fr 420px;
    gap: 64px;
    align-items: center;
  }

  .hero-handle {
    font-size: 12px;
    font-weight: 600;
    letter-spacing: 2px;
    color: var(--teal);
    margin-bottom: 24px;
  }

  .hero-title {
    font-size: clamp(40px, 6vw, 72px);
    font-weight: 800;
    line-height: 0.95;
    letter-spacing: -2px;
    margin-bottom: 24px;
  }

  .hero-title em {
    font-style: normal;
    color: var(--teal);
  }

  .hero-sub {
    font-size: 17px;
    color: var(--slate);
    line-height: 1.7;
    max-width: 480px;
    margin-bottom: 40px;
    font-weight: 400;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
  }

  .hero-proof {
    margin-top: 48px;
    display: flex;
    align-items: center;
    gap: 32px;
    flex-wrap: wrap;
  }

  .proof-item {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .proof-num {
    font-size: 28px;
    font-weight: 800;
    color: var(--teal);
    line-height: 1;
  }

  .proof-label {
    font-size: 11px;
    color: var(--slate);
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  .proof-divider {
    width: 1px;
    height: 40px;
    background: var(--grid);
  }

  /* EBOOK MOCKUP */
  .ebook-mockup {
    position: relative;
    display: flex;
    justify-content: center;
  }

  .ebook-card {
    width: 280px;
    background: var(--card);
    border: 1px solid rgba(0, 229, 212, 0.2);
    border-radius: 12px;
    overflow: hidden;
    position: relative;
    box-shadow:
      0 40px 80px rgba(0,0,0,0.6),
      0 0 60px rgba(0, 229, 212, 0.18);
  }

  .ebook-header {
    padding: 18px 18px 10px;
    border-bottom: 1px solid var(--grid);
  }

  .ebook-tag {
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 1px;
    color: var(--teal);
    text-transform: uppercase;
    margin-bottom: 6px;
  }

  .ebook-title {
    font-size: 18px;
    font-weight: 700;
    line-height: 1.3;
  }

  .ebook-body {
    padding: 18px;
  }

  .ebook-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 18px;
  }

  .ebook-price {
    display: flex;
    flex-direction: column;
    gap: 2px;
  }

  .price-label {
    font-size: 11px;
    color: var(--slate);
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .price-value {
    font-size: 20px;
    font-weight: 800;
    color: var(--teal);
  }

  .price-old {
    font-size: 12px;
    color: var(--slate);
    text-decoration: line-through;
    opacity: 0.7;
  }

  .ebook-badge {
    padding: 6px 10px;
    border-radius: 100px;
    background: rgba(0, 229, 212, 0.08);
    border: 1px solid rgba(0, 229, 212, 0.3);
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 1px;
  }

  .ebook-list {
    list-style: none;
    margin: 0 0 18px;
    padding: 0;
  }

  .ebook-list li {
    display: flex;
    align-items: center;
    gap: 8px;
    font-size: 12px;
    color: var(--slate);
    margin-bottom: 6px;
  }

  .ebook-list li::before {
    content: '•';
    color: var(--teal);
  }

  .ebook-footer {
    padding: 18px;
    border-top: 1px solid var(--grid);
    display: flex;
    flex-direction: column;
    gap: 8px;
    background: radial-gradient(circle at top, rgba(0, 229, 212, 0.12), transparent 60%);
  }

  .ebook-guarantee {
    font-size: 11px;
    color: var(--slate);
  }

  /* SECAO SOBRE O CONTEUDO */
  section {
    padding: 80px 32px;
  }

  .section-inner {
    max-width: 1100px;
    margin: 0 auto;
  }

  .two-cols {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 56px;
    align-items: flex-start;
  }

  .section-title {
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 16px;
  }

  .section-text {
    font-size: 16px;
    color: var(--slate);
    line-height: 1.8;
    margin-bottom: 24px;
  }

  .bullet-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .bullet-list li {
    display: flex;
    gap: 10px;
    margin-bottom: 14px;
    font-size: 14px;
    color: var(--slate);
  }

  .bullet-list li::before {
    content: '';
    width: 6px;
    height: 6px;
    border-radius: 50%;
    margin-top: 6px;
    background: var(--teal);
    flex-shrink: 0;
  }

  /* DEPOIMENTOS SIMPLES */
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0,1fr));
    gap: 24px;
    margin-top: 32px;
  }

  .testimonial-card {
    background: rgba(17,17,17,0.9);
    border-radius: 12px;
    border: 1px solid var(--grid);
    padding: 20px;
    font-size: 14px;
    color: var(--slate);
  }

  .testimonial-name {
    margin-top: 12px;
    font-size: 13px;
    color: var(--white);
    font-weight: 600;
  }

  /* FAQ */
  .faq-item {
    border-top: 1px solid var(--grid);
    padding: 18px 0;
  }

  .faq-question {
    font-size: 15px;
    font-weight: 600;
    margin-bottom: 8px;
  }

  .faq-answer {
    font-size: 14px;
    color: var(--slate);
    line-height: 1.7;
  }

  /* FOOTER */
  footer {
    border-top: 1px solid var(--grid);
    padding: 32px;
    font-size: 12px;
    color: var(--slate);
    text-align: center;
  }

  footer a {
    color: var(--teal);
    text-decoration: none;
  }

  footer a:hover { text-decoration: underline; }

  /* RESPONSIVO */
  @media (max-width: 900px) {
    nav {
      padding: 16px 20px;
    }

    .nav-links { display: none; }
    .hamburger { display: flex; }

    .hero {
      padding: 100px 20px 60px;
    }

    .hero-inner {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .ebook-mockup {
      order: -1;
    }

    section {
      padding: 60px 20px;
    }

    .two-cols {
      grid-template-columns: 1fr;
      gap: 32px;
    }

    .testimonials-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    .hero-title {
      font-size: 36px;
    }

    .hero-sub {
      font-size: 15px;
    }

    .btn-cta,
    .btn-outline {
      width: 100%;
      justify-content: center;
    }

    .hero-actions {
      flex-direction: column;
      align-items: stretch;
    }
  }
  </style>
</head>
<body>

  <!-- NAV -->
  <nav>
    <a href="#topo" class="nav-logo">
      <span>seu</span><span>Point</span>
    </a>

    <ul class="nav-links">
      <li><a href="#conteudo">Conteúdo</a></li>
      <li><a href="#para-quem">Para quem é</a></li>
      <li><a href="#faq">FAQ</a></li>
      <li><a href="#comprar" class="nav-cta">Garantir meu ebook</a></li>
    </ul>

    <button class="hamburger" id="hamburger" aria-label="Abrir menu">
      <span></span>
      <span></span>
      <span></span>
    </button>
  </nav>

  <div class="mobile-menu" id="mobileMenu">
    <a href="#conteudo">Conteúdo</a>
    <a href="#para-quem">Para quem é</a>
    <a href="#faq">FAQ</a>
    <a href="#comprar" class="btn-cta">Garantir meu ebook</a>
  </div>

  <!-- HERO -->
  <main id="topo">
    <section class="hero">
      <div class="hero-glow-tr"></div>
      <div class="hero-glow-bl"></div>

      <div class="hero-inner">
        <div>
          <div class="hero-handle">@seuPoint</div>

          <h1 class="hero-title">
            IA, economia e mercado em um <em>ebook prático</em>
          </h1>

          <p class="hero-sub">
            Um guia direto ao ponto para entender como a inteligência artificial está redesenhando o trabalho, os negócios e as oportunidades de quem se antecipa. Sem jargões, com aplicações reais.
          </p>

          <div class="hero-actions">
            <!-- SUBSTITUA O # PELO LINK DO SEU CHECKOUT (Hotmart, Kiwify etc.) -->
            <a href="#" class="btn-cta" id="comprar">
              Garantir meu ebook por R$ 46,70
            </a>

            <a href="#conteudo" class="btn-outline">
              Ver tudo que você vai aprender
            </a>
          </div>

          <div class="hero-proof">
            <div class="proof-item">
              <div class="proof-num">3</div>
              <div class="proof-label">Pilares IA + Economia</div>
            </div>

            <div class="proof-divider"></div>

            <div class="proof-item">
              <div class="proof-num">100%</div>
              <div class="proof-label">Focado em prática</div>
            </div>

            <div class="proof-divider"></div>

            <div class="proof-item">
              <div class="proof-num">&lt; 2h</div>
              <div class="proof-label">Para ler e aplicar</div>
            </div>
          </div>
        </div>

        <div class="ebook-mockup" aria-hidden="true">
          <div class="ebook-card">
            <div class="ebook-header">
              <div class="ebook-tag">Ebook digital</div>
              <div class="ebook-title">
                IA, Economia e Mercado<br>
                um mapa prático para aproveitar a virada
              </div>
            </div>

            <div class="ebook-body">
              <div class="ebook-meta">
                <div class="ebook-price">
                  <span class="price-label">Preço de lançamento</span>
                  <span class="price-value">R$ 46,70</span>
                  <span class="price-old">R$ 97,00</span>
                </div>
                <div class="ebook-badge">
                  Acesso imediato em PDF
                </div>
              </div>

              <ul class="ebook-list">
                <li>Entenda o básico de IA sem linguagem técnica</li>
                <li>Veja como a IA está mudando empregos e negócios</li>
                <li>Identifique oportunidades para ganhar dinheiro com IA</li>
                <li>Roteiro prático para aplicar IA na sua rotina</li>
              </ul>
            </div>

            <div class="ebook-footer">
              <a href="#" class="btn-cta">
                Quero acessar agora
              </a>
              <div class="ebook-guarantee">
                Conteúdo em formato PDF, acesso imediato após a confirmação do pagamento pelo seu email.
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CONTEUDO -->
    <section id="conteudo">
      <div class="section-inner">
        <div class="section-label">O que você encontra no ebook</div>

        <div class="two-cols">
          <div>
            <h2 class="section-title">
              O essencial que você precisa saber sobre IA e economia, sem perder tempo
            </h2>
            <p class="section-text">
              Este ebook foi pensado para quem quer entender o impacto da inteligência artificial na economia real e nas decisões do dia a dia. Você não precisa saber programar, só precisa estar disposto a atualizar a forma como pensa trabalho, carreira e negócios.
            </p>

            <ul class="bullet-list">
              <li>Panorama simples do que realmente é IA hoje e o que é só buzzword.</li>
              <li>Como as grandes mudanças tecnológicas se conectam com ciclos econômicos.</li>
              <li>Tipos de impacto da IA em profissões e setores diferentes.</li>
              <li>Erros mais comuns de quem tenta “surfear a onda” sem entender a lógica econômica.</li>
            </ul>
          </div>

          <div>
            <h3 class="section-title" style="font-size:20px;">
              Estrutura do ebook
            </h3>
            <ul class="bullet-list">
              <li>Capítulo 1 – Fundamentos de IA aplicados à economia.</li>
              <li>Capítulo 2 – IA, produtividade e reconfiguração do mercado de trabalho.</li>
              <li>Capítulo 3 – Oportunidades de negócio, risco e assimetria de informação.</li>
              <li>Capítulo 4 – Ferramentas práticas e exemplos de uso no dia a dia.</li>
              <li>Capítulo 5 – Como se posicionar para os próximos anos.</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- PARA QUEM E -->
    <section id="para-quem">
      <div class="section-inner">
        <div class="section-label">Para quem é</div>

        <div class="two-cols">
          <div>
            <h2 class="section-title">
              Para quem quer usar IA de forma produtiva, não só curiosa
            </h2>
            <p class="section-text">
              O conteúdo foi pensado para quem quer transformar IA em vantagem prática. Se você sente que está ficando para trás nas conversas sobre IA ou que está usando de forma superficial, este material organiza o mapa.
            </p>
          </div>

          <div>
            <ul class="bullet-list">
              <li>Profissionais que não são de tecnologia, mas precisam entender IA para tomada de decisão.</li>
              <li>Pessoas que querem proteger e reposicionar sua carreira na próxima década.</li>
              <li>Empreendedores e autônomos que querem aumentar produtividade e criar novas ofertas usando IA.</li>
              <li>Estudantes e curiosos que querem uma visão clara do impacto da IA na economia e no mercado.</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- DEPOIMENTOS (placeholder, você pode trocar depois) -->
    <section>
      <div class="section-inner">
        <div class="section-label">Feedback inicial</div>
        <h2 class="section-title">
          O que quem já leu destaca do material
        </h2>

        <div class="testimonials-grid">
          <div class="testimonial-card">
            O ebook conseguiu traduzir IA para a minha realidade de negócios. Saí com ideias aplicáveis, não só com conceitos soltos.
            <div class="testimonial-name">Leitora empreendedora</div>
          </div>
          <div class="testimonial-card">
            Gostei da parte de contexto econômico, ajuda a entender por que essa onda é diferente das anteriores e onde focar energia.
            <div class="testimonial-name">Profissional liberal</div>
          </div>
          <div class="testimonial-card">
            Em menos de duas horas consegui ler e já testar algumas aplicações de IA na minha rotina de estudos e trabalho.
            <div class="testimonial-name">Estudante e analista júnior</div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ -->
    <section id="faq">
      <div class="section-inner">
        <div class="section-label">FAQ</div>
        <h2 class="section-title">
          Dúvidas frequentes antes de garantir o ebook
        </h2>

        <div class="faq-item">
          <div class="faq-question">
            Como eu recebo o ebook depois de comprar?
          </div>
          <div class="faq-answer">
            Assim que o pagamento for confirmado pela plataforma de checkout, você recebe um email com o link para download do PDF. Em geral leva poucos minutos, dependendo do meio de pagamento escolhido.
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">
            O material é muito técnico?
          </div>
          <div class="faq-answer">
            Não. O foco é explicar os conceitos de IA conectando com economia, mercado e decisões práticas, em linguagem clara. Você não precisa saber programar, nem ter formação em tecnologia.
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">
            Por quanto tempo terei acesso ao conteúdo?
          </div>
          <div class="faq-answer">
            O ebook é em formato PDF, para você baixar e guardar nos seus dispositivos. Depois do download, o arquivo é seu para consultar quantas vezes quiser.
          </div>
        </div>

        <div class="faq-item">
          <div class="faq-question">
            Posso ler no celular?
          </div>
          <div class="faq-answer">
            Sim. O arquivo é um PDF leve, que abre normalmente em leitores padrão de celular, tablet e computador.
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- FOOTER -->
  <footer>
    <div>seuPoint – inteligência artificial aplicada à economia e ao mercado.</div>
    <div style="margin-top:8px;">
      Este site é uma landing page informativa para venda de um ebook digital.
    </div>
  </footer>

  <script>
    // Menu mobile simples
    const hamburger = document.getElementById('hamburger');
    const mobileMenu = document.getElementById('mobileMenu');

    if (hamburger && mobileMenu) {
      hamburger.addEventListener('click', () => {
        mobileMenu.classList.toggle('open');
      });

      mobileMenu.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          mobileMenu.classList.remove('open');
        });
      });
    }
  </script>
</body>
</html>